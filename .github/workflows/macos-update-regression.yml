name: macos-update-regression

on:
  push:
    branches: [studio-regression-ci]
    paths:
      - install.sh
      - studio/setup.sh
      - scripts/macos_update_repro.sh
      - scripts/log_spam_detector.py
      - unsloth_cli/**
      - .github/workflows/macos-update-regression.yml
  workflow_dispatch:
    inputs:
      from_sha:
        description: Older Studio SHA (good baseline)
        required: false
        default: 5c473fab
      to_sha:
        description: Target Studio SHA (current tip)
        required: false
        default: 07e2fccf

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  reproduce:
    runs-on: macos-14
    timeout-minutes: 60
    env:
      # Intentionally unset: install.sh skips macOS .app creation under
      # UNSLOTH_STUDIO_HOME, which is the regression we are trying to repro.
      UPDATE_FROM_SHA: ${{ inputs.from_sha || '5c473fab' }}
      UPDATE_TO_SHA: ${{ inputs.to_sha || '07e2fccf' }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run macOS update repro
        run: bash scripts/macos_update_repro.sh

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: macos-update-regression-${{ github.run_id }}
          # Default mode install lands under $HOME/.unsloth/studio.
          path: |
            ~/.unsloth/studio/share/studio.log
            ~/.unsloth/studio/share/studio.conf
            ~/.local/share/unsloth/studio.log
          if-no-files-found: ignore
